defmodule Rfx.Edit.Module.RenameModuleTest do
  use ExUnit.Case

  # TODO: add tests

  # alias Rfx.Ops.Module.RenameModule
  # alias Rfx.Util.Source

  describe "rename module placeholder" do
    test "always true" do
      assert true
    end
  end
end
