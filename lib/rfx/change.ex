defmodule Rfx.Change do

  # NEW - SAVE
  
  def apply(changeset) do
    changeset 
  end

  def apply!(changeset) do
    changeset 
  end

  def cast(changeset, _type) do
    changeset 
  end

  def cast!(changeset, _type) do
    changeset 
  end
end
